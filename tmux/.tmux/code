# create vim window
selectp -t 0
send "vim\n"

# create lower bar, 30%
split -v -p 30 -c "#{pane_current_path}"

# split bar in 2, run git status
split -h -c "#{pane_current_path}"
send "git fetch || git init\n"
send "git status\n"

# select vim, start nerd tree
selectp -t 0
send ":NERDTree\t\n"
